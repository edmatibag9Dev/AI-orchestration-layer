---
name: daily-ai-morning-briefing
description: Generate the owner's Daily Morning AI Briefing: research, publish site, test theses, distribute to Open Brain + Day One, push to GitHub
---

Run the Daily Morning AI Briefing pipeline. Work in ~/Documents/Claude/Projects/ai-briefing/.

FIRST ACTION (stall detection): run `date '+%Y-%m-%dT%H:%M'` and compare to the 7:45am schedule. If the current time is more than 30 minutes past 7:45am local, this run stalled (usually a permission prompt) or fired late (app was closed) — append a line to data/runs.log NOW recording it, e.g. "2026-06-12T11:20 | edition=N | stall | run scheduled 07:45, actually executing now; likely blocked on tool approval or app closed". Then proceed normally. Do the same check again just before publishing: if more than 1 hour elapsed between your first action and publish, log a second stall line with the gap. The run history must never have silent gaps.

RUN HYGIENE (so this unattended run never stalls on an approval prompt): permissions are pre-approved via a scoped allow-list in ~/.claude/settings.json (the home-rooted run is gated ONLY by the global file; the repo's .claude/settings.json is a mirror — keep both in sync). To stay inside that allow-list, prefer SIMPLE single-command Bash calls with ABSOLUTE paths (e.g. `git -C ~/Documents/Claude/Projects/ai-briefing <cmd>`, absolute file paths) over `cd … && …` chains. If a tool call ever blocks on a permission prompt, one command isn't on the allow-list — record it in data/runs.log so the operator can add that single rule to BOTH settings files; never fabricate around a block. Already allowed: WebSearch/WebFetch, Read/Edit/Write, Bash(git/rsync/python3/curl + shell utils), open-brain/dayone MCP writes, PushNotification.

AUTHORITATIVE INSTRUCTIONS: Read pipeline/briefing-prompt.md FIRST and follow it exactly — it is the versioned editorial spec and overrides anything abbreviated here. Summary of the flow:

1. Read data/seen-stories.json (dedup memory) and pipeline/sources.json (Discourse source roster). Do NOT read pipeline/theses.md yet — research is blind by design.
2. RESEARCH (blind): spawn parallel research agents (or run sequentially if subagents lack web access) covering the past 24 hours across: Frontier Labs, Open Source, Industry & Capital, The Discourse (fetch HN Algolia API, Reddit RSS .rss endpoints, YouTube channel RSS feeds from sources.json, plus web search for secondhand X coverage), and AI in Business. ~25-40 searches/fetches total. Hard rules: real URLs only, never invent items; every published item needs a CONFIRMED date inside the 24h window (one follow-up search to verify if needed; drop if unconfirmable); never pad a slow day.
3. SYNTHESIZE per the spec: dedup vs seen-stories (material updates = "↗ Developing" linking prior coverage); significance filter ("would an investor or SaaS presales engineer act differently?"); max 5 executive items with takes on the AI thesis only; 15-25 scan items; tickers as $SYM in IBM Plex Mono hyperlinked to https://finance.yahoo.com/quote/SYM — EVERYWHERE a symbol appears, including inside "Why it matters" take body text (done-gate assertion 7 mechanically fails bare tickers); for platform events dig past keynotes into developer/architecture components and name them.
4. THESIS TESTING: now read pipeline/theses.md. For each ACTIVE thesis ask in order: (a) does today's evidence CUT AGAINST it? (b) support it? (c) silence = log nothing. Refuting evidence leads takes ("⚠ cuts against thesis...") and outranks confirming evidence for Layer-1 placement. Append dated CONFIRMS/REFUTES entries to evidence logs in theses.md. If kill criteria are met, set status to weakening/broken and flag prominently. Never add or retire theses yourself — only the owner does.
5. PUBLISH: write index.html as a fully SELF-CONTAINED page (inline brand-token CSS, inline EM SVG logo, data-URI favicon — follow the existing index.html as the template exactly: Fraunces/Inter/IBM Plex Mono, teal #2C7A6B / navy #2B4C7E, light+dark themes, staleness banner with today's GENERATED date, header links to theses.html and archive/index.html). Copy to archive/YYYY-MM-DD.html with sed rewrites: href="archive/index.html" → "index.html" and href="theses.html" → "../theses.html". Add the edition to archive/index.html. Update theses.html dashboard (statuses, evidence timelines, confirm/refute counts, last-tested dates). Update data/seen-stories.json (add today's stories with claims/category/layer/published fields; prune entries older than 30 days). Append a run entry to data/runs.log (edition #, searches, item counts, status).
6. DISTRIBUTE (never block publish on failures; log failures to runs.log and note on the page): (a) capture each Layer-1 executive item to Open Brain via capture_thought as a standalone thought: "[AI Briefing YYYY-MM-DD · Category] Headline. Summary. Why it matters: take. Tickers: $X. Source: URL — tags: ai-briefing, category, date". (b) Create a Day One entry in journal "AI News Daily Briefing" titled "AI Morning Briefing — Day, Mon DD, YYYY", tags ai-briefing + date: full briefing as markdown using lists not tables, tickers as [$SYM](yahoo link).
7. GIT: read CONTRIBUTING.md and follow it — commit with full format (type(scope) subject + body with ≥3 bullets; type is "data" for a routine daily edition), update README.md only if files/behavior changed, end commit message with "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>", push to origin main, verify the push succeeded.
8. JUDGE (shadow, non-blocking — spec pipeline step 10): after the done gate passes, run exactly:
   `python3 $HOME/Documents/Claude/Projects/AI-orchestration-layer/checks/judge.py --rubric $HOME/Documents/Claude/Projects/AI-orchestration-layer/rubrics/morning-briefing.md --artifact $HOME/Documents/Claude/Projects/ai-briefing/archive/<YYYY-MM-DD>.html --judge-model openrouter/z-ai/glm-5.2 --shadow`
   Shadow mode always exits 0 and NEVER gates, delays, or degrades the run. Append `judge=<score>` (e.g. `judge=0.90`) or `judge=error` to today's runs.log entry. On any judge error or timeout: log it and carry on — the briefing outcome is unaffected.
9. PING (final step, after the push is verified) — notify the owner, Slack-webhook-primary with a desktop/phone fallback. Send a COMPACT message that reflects the TRUE outcome, never a default "✅".
   - PRIMARY — Slack webhook (posts to **#ai-briefing** under the "Claude Alerts" app identity, which is what makes Slack actually banner/push; verified 2026-08-04). Pipe the message via stdin:
     `printf '%s' '<message>' | python3 $HOME/.claude/lib/slack_alert.py ai-briefing -`
     The script always exits 0 and prints `alert-sent ai-briefing` or `alert-failed ai-briefing: <reason>` — treat only `alert-sent` as delivered. Compact format (Slack mrkdwn — *bold* is single-asterisk, links are `<url|text>`; render $TICKERs as plain `$SYM`):
     `📰 *AI Briefing #N — Day, Mon DD* · <STATUS> (<M> exec items, git pushed, judge <score|—>)`
     `*Top:* <top executive headline>`
     `📖 Full brief: Day One › "AI News Daily Briefing"  ·  🏠 iPad (home wifi): http://ed-macbook-pro-m1x.local:8765`
     where `<STATUS>` is `✅ published` | `⚠️ published with caveats — <what failed>, see runs.log` | `❌ did not run — <why>`.
     DO NOT "simplify" back to the Slack MCP connector (`slack_send_message`): the connector posts AS the owner himself, and Slack never notifies a user of their own messages — every connector post lands silently (diagnosed 2026-08-04; this channel was silent for weeks that way). The webhook secret lives at `~/.config/claude-alerts/ai-briefing_webhook` (file, not env var — headless runs don't source ~/.zshrc). Never print or log the webhook URL.
   - FALLBACK — push: if the webhook reports `alert-failed`, send the same one-liner (under 200 chars, no markdown) via PushNotification (load via ToolSearch "select:PushNotification").
   - Record in data/runs.log which fired (`ping=webhook` | `ping=push` | `ping=both-failed`). NEVER block or fail the run on a ping error — if a notification reports it wasn't sent, that's expected; carry on.

Success criteria: index.html shows today's date; archive has today's edition; theses.md and theses.html reflect today's testing; Open Brain has the top stories; Day One has the entry; git push verified; judge shadow score (or judge=error) logged in runs.log; any stall is recorded in runs.log; a ping (webhook to #ai-briefing, else PushNotification) was sent reflecting the true outcome and which-fired is logged. If web access is unavailable, do NOT fabricate a briefing — log the failure to runs.log, ping the failure (Slack/push), and stop (the staleness banner will inform the owner).
---

ATTENTION-LAYER FOOTER (per ESCALATION-POLICY.md, added 2026-07-27 with the owner's approval):

1. Noteworthy but NON-BLOCKING findings from this run (skipped/malformed inputs, auth warnings, source-format drift, anything the owner should eventually see but that shouldn't interrupt him) → append one Lane-2 JSON line to ~/Documents/Claude/Projects/AI-orchestration-layer/runs/digest.jsonl:
   {"ts": "<ISO-8601 local>", "severity": "info|minor", "category": "<short-kebab>", "text": "<standalone description>", "source": "daily-ai-morning-briefing", "status": "new"}
   Append-only: never edit, re-deliver, or delete existing rows — the evening-digest task owns delivery and status transitions. Do not file duplicates of an item already in the queue.

2. If you CANNOT complete this job, say so explicitly in your final report AND file a Lane-2 row describing what failed (severity "minor"; use "major" only if data was lost).

3. ALWAYS end the run — success or failure — by appending one heartbeat line to ~/Documents/Claude/Projects/Mission-Control-Dashboard/runs/heartbeat.jsonl:
   {"task": "daily-ai-morning-briefing", "ts": "<ISO-8601 local>", "status": "ok|partial|failed", "note": "<one line>"}
   The ops-watcher reads this to distinguish a run that completed from one that started and died.
